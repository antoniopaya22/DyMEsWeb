---
import CompendioLayout from '../../../layouts/CompendioLayout.astro';
import { BACKGROUNDS } from '../../../data/backgrounds';
---

<CompendioLayout title="Trasfondos — Compendio SRD 5.1" section="trasfondos" breadcrumb="Trasfondos">
  <section class="docs-section scroll-mt-20">
    <div class="docs-section-header">
      <span class="docs-section-badge"><i class="fa-solid fa-scroll"></i> Trasfondos</span>
      <h2 class="docs-h2">Trasfondos</h2>
      <p class="docs-p text-[#555137]">Los trasfondos describen el origen de tu personaje antes de la aventura, otorgando habilidades, equipo y rasgos narrativos.</p>
    </div>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6">
      {BACKGROUNDS.map((bg) => (
        <a href={`/compendio/trasfondos/${bg.id}`} class="docs-item-card group">
          <span class="docs-item-card-icon"><i class={bg.icon}></i></span>
          <div class="docs-item-card-body">
            <h3 class="docs-item-card-title">{bg.nombre}</h3>
            <p class="docs-item-card-desc">{bg.descripcion}</p>
          </div>
          <div class="docs-item-card-meta">
            {bg.details.slice(0, 2).map(d => (
              <span class="docs-item-card-tag">{d.label}: {d.value}</span>
            ))}
          </div>
          <span class="docs-item-card-arrow">→</span>
        </a>
      ))}
    </div>
  </section>
</CompendioLayout>
