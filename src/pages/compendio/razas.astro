---
import CompendioLayout from '../../layouts/CompendioLayout.astro';
import { RACES } from '../../data/races';
---

<CompendioLayout title="Razas — Compendio SRD 5.1" section="razas" breadcrumb="Razas">

  <section class="docs-section scroll-mt-20">
    <div class="docs-section-header">
      <span class="docs-section-badge">🧝 Razas</span>
      <h2 class="docs-h2">Razas jugables</h2>
      <p class="docs-p text-[#555137]">Las razas disponibles en el SRD 5.1 para la creación de personajes. Cada raza otorga bonificaciones, rasgos y habilidades únicas.</p>
    </div>

    {RACES.map((entry, i) => (
      <article id={entry.id} class="docs-entry scroll-mt-20" data-section-id={entry.id}>
        <div class="docs-entry-header">
          <span class="docs-entry-icon">{entry.icon}</span>
          <div>
            <h3 class="docs-h3">{entry.nombre}</h3>
            <span class="docs-tag">Raza</span>
          </div>
          <a href={`#${entry.id}`} class="docs-anchor" title="Enlace directo">#</a>
        </div>
        <p class="docs-p">{entry.descripcion}</p>
        <div class="docs-stats-grid">
          {entry.details.map(d => (
            <div class="docs-stat">
              <dt class="docs-stat-label">{d.label}</dt>
              <dd class="docs-stat-value">{d.value}</dd>
            </div>
          ))}
        </div>

        {/* Rasgos raciales */}
        {entry.rasgos && entry.rasgos.length > 0 && (
          <div class="docs-traits">
            <h4 class="docs-h4">Rasgos raciales</h4>
            {entry.rasgos.map(r => (
              <div class="docs-trait">
                <span class="docs-trait-name">{r.nombre}.</span>
                <span class="docs-trait-desc">{r.desc}</span>
              </div>
            ))}
          </div>
        )}

        {/* Subraza */}
        {entry.subraza && (
          <div class="docs-subrace">
            <h4 class="docs-h4">Subraza: {entry.subraza.nombre}</h4>
            <p class="docs-p">{entry.subraza.desc}</p>
            <p class="docs-p" style="margin-top: 0.25rem;"><strong>Rasgos:</strong> {entry.subraza.rasgos}</p>
          </div>
        )}

        {i < RACES.length - 1 && <div class="docs-entry-divider"></div>}
      </article>
    ))}
  </section>

</CompendioLayout>
